version: 1.0
provider:
  name: openfaas
  gateway: https://veba.corp.local
functions:
  vm-reconfig-via-tag-fn:
    lang: golang-http
    handler: ./handler
    image: fgold/veba-go-vm-reconfig-via-tag:1
    environment:
      write_debug: true
    secrets:
      - vcconfig
    annotations:
      topic: VmPoweredOffEvent
