version: 1.0
provider:
  name: openfaas
  gateway: https://veba.yourdomain.com
functions:
  tag-vm-config-fn:
    lang: golang-http
    handler: ./handler
    image: fgold/veba-go-tag-vm-config:1
    environment:
      write_debug: true
    secrets:
      - vcconfig
    annotations:
      topic: AlarmStatusChangedEvent
